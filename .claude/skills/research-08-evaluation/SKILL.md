---
name: research-08-evaluation
description: 評価実験の設計・実施・分析。ベースライン比較・データセット選定・実験計画(事前登録)・統計解析・グラフ作成・考察・限界の整理を行う。ユーザーが「実験したい」「評価したい」「ベースラインと比較したい」「実験結果をまとめたい」「統計解析したい」と言ったとき、または research-07-implementation の次のステップとして使うこと。
model: sonnet
---

# 評価実験(Evaluation)

提案手法の性能を統計的に信頼できる形で評価し、レポートにまとめる。

## 前提

開始時に `research/STATUS.md`・`research/evaluation-metrics.md`・`research/hypotheses.md`・`research/baseline-summary.md`・`research/implementation-plan.md` を読む。
実装コードの有無と実行環境を確認する。

## 進め方

### 1. 実験計画の事前登録(実行前に必ず書く)

**結果を見る前に**実験計画を `research/evaluation-report.md` の冒頭に確定させる。後から都合よく評価条件を変えないための予防措置:

- 検証する仮説(hypotheses.md への参照)と判定基準
- データセット(分割方法、前処理、リーク防止の確認)
- ベースライン(バージョン・設定を固定)
- 評価指標と測定方法(evaluation-metrics.md と一致させる)
- **乱数シード: 最低 3 つ**(可能なら 5)で複数回実行
- ハイパーパラメータ探索の範囲と方法(提案手法とベースラインに同等の探索予算を割く)
- 統計検定の選択(対応の有無・正規性を考慮)と有意水準

### 2. 実験の実施

自分で書いたコードは、動けば正しいと思い込みやすい。指標の定義ミス・データリーク・
判定ロジックの誤りは実行時エラーにならず、統計的に有意な結果に見えるまま静かに紛れ込む。
実装者本人の見直しだけでは防ぎにくいため、過去の会話コンテキストを渡さない独立した
サブエージェントに客観的にレビューさせる。手順・プロンプト例は `references/code-review.md` を参照。

- 主要コード(学習ループ・指標計算・データ分割・統計検定など)を書いた/変更した直後、
  実行する前に **`references/code-review.md` の Stage 1(設計適合・バグの独立レビュー)** を通す。
  指摘があれば直してから先に進む
- 極小データでのドライランで配線を確認してから本実験を回す
- コードが完成し実データに投入する前に、**`references/code-review.md` の Stage 2**
  (成功するはずのダミーケース/絶対に失敗するはずのダミーケースでの判定ロジックの妥当性レビュー)
  を通す。指摘があればコードを直し、Stage 1 からやり直す
- 長時間ジョブは Bash の `run_in_background` で実行し、途中経過を監視する
- **複数条件・複数シードを並行実行する場合、各ジョブの出力先は条件ごとに別ファイル
  (例: `research/logs/seed{N}-{condition}.log`)にする。** 複数のバックグラウンドジョブが
  `research/experiment-results.md` に同時に追記すると、書き込みが競合して内容が消失・破損する。
  `research/experiment-results.md` への集約は、各ジョブの完了を確認したうえで自分(本体)が
  一つずつ読み取って書き足す形にする(並行書き込みをさせない)
- 生の結果(全シード・全条件)を `research/experiment-results.md` に追記していく。失敗した実行も原因とともに記録する

### 3. 統計解析と可視化

- 各条件: 平均 ± 標準偏差(または信頼区間)を報告
- ベースラインとの差: 統計検定の p 値 + **効果量**(差の大きさ)。「有意だが微小」を見分ける
- グラフ作成(matplotlib 等): 学習曲線、条件別比較、指標のトレードオフ図。軸ラベル・単位・凡例を必ず付ける
- 図は `research/figures/` に保存(論文・スライドで再利用する)

### 4. 考察と限界

- 仮説は支持されたか棄却されたかを明言する(グレーなら「判定不能」とその理由)
- 予想と違った結果の原因分析(悪い結果を隠さない — アブレーションと査読対応の材料になる)
- 限界の整理: 評価できていない条件、一般化の範囲、脅威(threats to validity)
- **結論を確定する前に `references/code-review.md` の Stage 3(実データ適用後・結果の妥当性の
  独立レビュー)を通す。** 「うまくいった」結果もコードのバグ・データリークで説明できないか、
  「うまくいかなかった」結果も実装のバグではなく手法自体の限界と言い切れるかを、独立した
  サブエージェントに検証させる。コードの問題が疑われれば直して Stage 1 からやり直し
  (実験の再実行が必要)、手法自体の限界であればここに正直に反映する

### 5. 出力

```
research/
    evaluation-report.md    # 実験計画(事前登録)、結果サマリ表、統計解析、考察、限界
    experiment-results.md   # 生データ: 全実行の条件・シード・結果・ログ参照
    figures/                # グラフ
```

### 6. 完了処理

- `research/STATUS.md` を更新(仮説の判定結果を記録)
- 次のステップ `/research-09-ablation-study` を案内

## 品質基準

- 実験コードは自己レビューだけで進めない(`references/code-review.md` の3段階レビューを飛ばさない)
- シード 1 つの結果で結論を出さない
- ベースラインの数値は自環境での再実行値を第一とし、論文からの引用値を使う場合はその旨を明記する
- 表の数値は experiment-results.md の生データから機械的に導出できること(手作業の転記ミスを防ぐため集計スクリプトを書く)
